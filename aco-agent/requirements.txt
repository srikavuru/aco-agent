# ACO Agent — Azure Function dependencies

azure-functions>=1.21.3
azure-cosmos>=4.7.0

# Semantic similarity engine
# CPU-only torch — keeps the package under 500MB for Consumption plan
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.9.0
sentence-transformers>=3.3.1
numpy>=1.26.4
